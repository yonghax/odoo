<openerp>
    <data>
        <record model="ir.ui.view" id="view_partner_form_inherit">
            <field name="name">res_partner_form_inherit</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref='base.view_partner_form'></field>
            <field name="arch" type="xml">
                <field name="user_id" position="before">
                    <field name="outlet_type" attrs="{'invisible': [('customer', '=', False)]}"/> 
                    <field name="consignment" attrs="{'invisible': [('customer', '=', False)]}"/>
                </field>
            </field>
        </record>

        <!-- <record model="ir.ui.view" id="view_partner_stock_form_inherit">
            <field name="name">res.partner.stock.property.form.inherit</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref='stock.view_partner_stock_form'></field>
            <field name="arch" type="xml">
                <field name="property_stock_customer" position="before">
                    <field name="consignment_location_ids" widget="many2many_tags" options="{'no_create': True}" 
                        attrs="{'invisible': [('consignment', '=', False), ('customer', '=', False)]}" />
                </field>
            </field>
        </record> -->
    </data>
</openerp>